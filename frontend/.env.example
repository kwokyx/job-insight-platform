# 复制为 .env.local（.env.local 已被 gitignore，不会提交）后按需修改。
# 所有变量都有合理默认值，队友如果后端就跑在本机 8080，可以直接 npm run dev。

# ─── 开发时 Vite 代理目标 ───
# Vite 把 /api/* 转发到这里。三种典型值：
#   http://localhost:8080      后端跑在宿主机（Docker compose 暴露 8080 或本地 spring-boot:run）
#   http://localhost:18080     后端容器换了宿主端口
#   http://192.168.x.x:8080    后端在局域网另一台机器
VITE_API_PROXY_TARGET=http://localhost:8080

# ─── Vite 开发服务器端口 ───
VITE_DEV_PORT=5173

# ─── 运行时 API 前缀（一般不用改）───
# 前端所有请求拼在这个前缀后面。留空会用默认 /api/v1，走 Vite 代理到后端。
# 如果想让浏览器直连后端（跳过代理），可改成 http://localhost:8080/api/v1
# VITE_API_BASE=/api/v1
# VITE_ALGO_BASE=
